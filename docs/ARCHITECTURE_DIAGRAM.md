# Diagrama de Arquitectura: Sistema de Slots

**Documentación visual de la arquitectura del nuevo sistema**

---

## 📊 Vista General del Sistema

```
┌─────────────────────────────────────────────────────────────────┐
│                    CLIENTE (Google Apps Script,                 │
│                    HTTP Client, Browser, etc.)                  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP POST /api/v1/generate-packet
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FASTAPI LAYER                              │
│                   (app/api/v1/packet.py)                        │
│                                                                 │
│  • Valida request (PacketRequest)                              │
│  • Selecciona orquestador (slot-based vs legacy)              │
│  • Maneja errores HTTP                                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ use_legacy=False (default)
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  SLOT ORCHESTRATOR                              │
│            (app/services/slot_orchestrator.py)                  │
│                                                                 │
│  ┌─────────────────────────────────────────────────┐          │
│  │ 1. Cargar Manifest                              │          │
│  │ 2. Obtener Token de Dropbox                     │          │
│  │ 3. Para cada slot en manifest:                  │          │
│  │    → Ejecutar SlotResolver                      │          │
│  │    → Recopilar archivos                         │          │
│  │ 4. Convertir imágenes a PDF                     │          │
│  │ 5. Generar contenido para slots "generated"     │          │
│  │ 6. Ensamblar PDF final con PDFAssembler        │          │
│  │ 7. Subir a Google Drive                         │          │
│  │ 8. Actualizar Google Sheets                     │          │
│  │ 9. Retornar PacketResponse                      │          │
│  └─────────────────────────────────────────────────┘          │
└─────┬─────────────────────────────────────────┬─────────────────┘
      │                                         │
      │ usa                                     │ usa
      ▼                                         ▼
┌──────────────────────┐              ┌──────────────────────┐
│   SLOT RESOLVER      │              │   PDF ASSEMBLER      │
│  (slot_resolver.py)  │              │  (pdf_assembler.py)  │
│                      │              │                      │
│ • Resuelve slots     │              │ • merge_pdfs()       │
│ • Ejecuta            │              │ • create_cover()     │
│   estrategias        │              │ • append_cover()     │
│ • Descarga archivos  │              │                      │
│ • Retorna            │              │ Backend limpio       │
│   SlotResult         │              │ (solo PDF ops)       │
└──────┬───────────────┘              └──────────────────────┘
       │
       │ lee configuración de
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│                     MANIFEST LAYER                           │
│                  (slot_models.py)                            │
│                                                              │
│  ┌────────────────┐  ┌────────────────┐  ┌───────────────┐│
│  │ PacketManifest │  │     Slot       │  │SearchStrategy ││
│  │                │  │                │  │               ││
│  │ • name         │  │ • slot_id      │  │ • type        ││
│  │ • version      │  │ • name         │  │ • keywords    ││
│  │ • slots[]      │  │ • required     │  │ • mode        ││
│  └────────────────┘  └────────────────┘  └───────────────┘│
└──────────────────────────────────────────────────────────────┘
       │
       │ implementación default
       ▼
┌──────────────────────────────────────────────────────────────┐
│              VAWA DEFAULT MANIFEST                           │
│           (vawa_default_manifest.py)                         │
│                                                              │
│  Slot 1: USCIS Documents        (required)                  │
│  Slot 2: Missing Report         (generated)                 │
│  Slot 3: VAWA Evidence          (optional)                  │
│  Slot 4: Filed Copy             (required, prioritized)     │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Datos Detallado

### 1. Request Inicial

```
Cliente
  │
  └─► POST /api/v1/generate-packet
      {
        "client_name": "Juan Perez",
        "dropbox_url": "https://...",
        "drive_parent_folder_id": "..."
      }
```

### 2. Procesamiento del Orquestador

```
SlotBasedOrchestrator.process_request()
  │
  ├─► 1. Cargar manifest
  │     └─► get_vawa_default_manifest()
  │
  ├─► 2. Obtener token
  │     └─► TokenServiceClient.get_valid_token()
  │
  ├─► 3. Resolver cada slot
  │     │
  │     ├─► Slot 1 (USCIS)
  │     │     └─► SlotResolver.resolve_slot()
  │     │           ├─► DropboxIntegrator.find_folder_fuzzy()
  │     │           ├─► DropboxIntegrator.find_files_recursive_fuzzy()
  │     │           └─► DropboxIntegrator.download_file()
  │     │
  │     ├─► Slot 2 (Missing Report)
  │     │     └─► SlotResolver._resolve_generated()
  │     │
  │     ├─► Slot 3 (Evidence)
  │     │     └─► SlotResolver._resolve_recursive_download()
  │     │
  │     └─► Slot 4 (Filed Copy)
  │           └─► SlotResolver._resolve_prioritized_search()
  │
  ├─► 4. Convertir imágenes
  │     └─► PDFEngine.convert_images_to_pdf_recursive()
  │
  ├─► 5. Generar reportes
  │     └─► _create_missing_report_pdf()
  │
  ├─► 6. Ensamblar PDF
  │     └─► PDFAssembler.merge_pdfs_in_order()
  │           ├─► create_cover_page() (para cada slot)
  │           └─► merge PDFs en orden
  │
  ├─► 7. Subir a Drive
  │     └─► GoogleIntegrator.upload_file()
  │
  └─► 8. Retornar respuesta
        └─► PacketResponse {
              status: "success",
              drive_folder_link: "...",
              final_pdf_link: "...",
              missing_files: []
            }
```

---

## 🎯 Componentes por Responsabilidad

### API Layer
```
┌─────────────────────────────────┐
│   app/api/v1/packet.py          │
│                                 │
│ Responsabilidades:              │
│ • Validar request HTTP          │
│ • Routing                       │
│ • Seleccionar orquestador       │
│ • Manejo de errores HTTP        │
│ • Logging de requests           │
└─────────────────────────────────┘
```

### Orchestration Layer
```
┌──────────────────────────────────────┐
│   app/services/slot_orchestrator.py │
│                                      │
│ Responsabilidades:                   │
│ • Coordinar proceso completo         │
│ • Gestionar ciclo de vida            │
│ • Integrar componentes               │
│ • Generar reportes                   │
│ • Cleanup de temporales              │
└──────────────────────────────────────┘
```

### Resolution Layer
```
┌────────────────────────────────────┐
│   app/services/slot_resolver.py   │
│                                    │
│ Responsabilidades:                 │
│ • Ejecutar estrategias búsqueda    │
│ • Descargar archivos               │
│ • Retornar SlotResult              │
│ • NO sabe de PDF assembly          │
└────────────────────────────────────┘
```

### PDF Layer
```
┌───────────────────────────────────┐
│   app/services/pdf_assembler.py  │
│                                   │
│ Responsabilidades:                │
│ • Unir PDFs con pypdf             │
│ • Crear portadas                  │
│ • Operaciones PDF puras           │
│ • NO sabe de Dropbox/Drive        │
└───────────────────────────────────┘
```

### Model Layer
```
┌─────────────────────────────────┐
│   app/services/slot_models.py  │
│                                 │
│ Responsabilidades:              │
│ • Definir estructuras de datos  │
│ • Validación con Pydantic       │
│ • Tipos de estrategias          │
│ • Enums y constantes            │
└─────────────────────────────────┘
```

### Integration Layer
```
┌──────────────────────────────────────────┐
│   app/integrations/                      │
│   ├── dropbox_client.py                  │
│   ├── google_client.py                   │
│   └── token_client.py                    │
│                                          │
│ Responsabilidades:                       │
│ • APIs externas (Dropbox, Drive)        │
│ • Autenticación                          │
│ • Rate limiting                          │
│ • Retry logic                            │
└──────────────────────────────────────────┘
```

---

## 🔀 Estrategias de Búsqueda

```
┌─────────────────────────────────────────────────────────────┐
│                    ESTRATEGIAS SOPORTADAS                   │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  1. FOLDER_SEARCH                            │
│                                              │
│  Base Path                                   │
│     │                                        │
│     └─► find_folder_fuzzy(keywords)         │
│           │                                  │
│           └─► find_files(file_keywords)     │
│                 │                            │
│                 └─► [file1.pdf, file2.pdf]  │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  2. RECURSIVE_DOWNLOAD                       │
│                                              │
│  Base Path                                   │
│     │                                        │
│     └─► Navigate: folder_path[]             │
│           │                                  │
│           └─► download_all_recursive()      │
│                 │                            │
│                 └─► [all files in tree]     │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  3. PRIORITIZED_SEARCH                       │
│                                              │
│  Base Path                                   │
│     │                                        │
│     └─► find_folder_fuzzy()                 │
│           │                                  │
│           └─► for keyword in priority:      │
│                 ├─► try keyword[0] ✓        │
│                 │     └─► FOUND! Return     │
│                 ├─► try keyword[1] ✗        │
│                 └─► try keyword[2] ✗        │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  4. GENERATED                                │
│                                              │
│  No búsqueda en Dropbox                     │
│     │                                        │
│     └─► generator="missing_report"          │
│           │                                  │
│           └─► create_missing_report_pdf()   │
│                 │                            │
│                 └─► [generated.pdf]         │
└──────────────────────────────────────────────┘
```

---

## 📦 Estructura del PDF Final

```
┌─────────────────────────────────────────────────────┐
│              PAQUETE_ENSAMBLADO.pdf                 │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ [PORTADA]                                 │    │
│  │ 1. EXHIBIT – Juan Perez                   │    │
│  │    USCIS Documents                        │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ Prima_Facie.pdf                           │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ I-360_Transfer_Notice.pdf                 │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ [PORTADA]                                 │    │
│  │ 2. EXHIBIT – Missing Documents Report     │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ Missing_Report.pdf (generado)             │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ [PORTADA]                                 │    │
│  │ 3. EXHIBIT – VAWA Evidence                │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ evidence_1.pdf                            │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ evidence_2.pdf                            │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ [PORTADA]                                 │    │
│  │ 4. EXHIBIT – Filed Copy                   │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
│  ┌───────────────────────────────────────────┐    │
│  │ Filed_Copy.pdf                            │    │
│  └───────────────────────────────────────────┘    │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 🔐 Flujo de Autenticación

```
┌─────────────────────────────────────────────────────┐
│            GESTIÓN DE TOKENS                        │
└─────────────────────────────────────────────────────┘

Opción 1: Token en Request
┌──────────────┐
│   Cliente    │
│              │
│ Proporciona  │
│ Dropbox      │
│ Token        │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Orquestador      │
│ Usa token        │
│ directamente     │
└──────────────────┘


Opción 2: Sin Token (Default)
┌──────────────┐
│   Cliente    │
│              │
│ No envía     │
│ token        │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────┐
│ TokenServiceClient          │
│                             │
│ GET /api/v1/token           │
│   + signature               │
│   + client_name             │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ AccessTokenDropbox Service  │
│ (Microservicio externo)     │
│                             │
│ Retorna token válido        │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ Orquestador                 │
│ Usa token obtenido          │
└─────────────────────────────┘
```

---

## 📊 Modelo de Datos

```
┌─────────────────────────────────────────────────────┐
│                 JERARQUÍA DE MODELOS                │
└─────────────────────────────────────────────────────┘

PacketManifest
  │
  ├─► name: str
  ├─► version: str
  ├─► description: str
  └─► slots: List[Slot]
        │
        └─► Slot
              ├─► slot_id: int
              ├─► name: str
              ├─► required: bool
              ├─► cover_page: bool
              └─► search_strategy: SearchStrategy
                    │
                    ├─► type: SearchStrategyType
                    ├─► folder_keywords: List[str]
                    ├─► file_keywords: List[str]
                    ├─► mode: SearchMode
                    └─► metadata: Dict


SlotResult (Output)
  │
  ├─► slot_id: int
  ├─► name: str
  ├─► files_found: List[str]
  ├─► status: "success" | "partial" | "missing"
  ├─► error_message: str
  └─► required: bool


AssemblyReport (Final Output)
  │
  ├─► success: bool
  ├─► total_slots: int
  ├─► completed_slots: int
  ├─► missing_required_slots: List[str]
  ├─► slot_results: List[SlotResult]
  └─► final_pdf_path: str
```

---

## 🚀 Ciclo de Vida de un Request

```
Time ───────────────────────────────────────────────────►

  0s │ Request recibido
     │ ┌──────────────────┐
     │ │ FastAPI Endpoint │
     │ └──────────────────┘
     │
  1s │ Cargar manifest + Obtener token
     │ ┌────────────────────────────┐
     │ │ SlotBasedOrchestrator init │
     │ └────────────────────────────┘
     │
  5s │ Resolver Slot 1 (USCIS)
     │ ┌────────────────────────────┐
     │ │ • Buscar carpeta           │
     │ │ • Encontrar 3 archivos     │
     │ │ • Descargar                │
     │ └────────────────────────────┘
     │
 15s │ Resolver Slot 2 (Missing Report)
     │ ┌────────────────────────────┐
     │ │ • Generado (sin descarga)  │
     │ └────────────────────────────┘
     │
 30s │ Resolver Slot 3 (Evidence)
     │ ┌────────────────────────────┐
     │ │ • Búsqueda recursiva       │
     │ │ • Encontrar 15 archivos    │
     │ │ • Descargar todos          │
     │ └────────────────────────────┘
     │
 60s │ Resolver Slot 4 (Filed Copy)
     │ ┌────────────────────────────┐
     │ │ • Búsqueda priorizada      │
     │ │ • Encontrar 1 archivo      │
     │ │ • Descargar                │
     │ └────────────────────────────┘
     │
 70s │ Convertir imágenes a PDF
     │ ┌────────────────────────────┐
     │ │ • 5 JPG → PDF              │
     │ └────────────────────────────┘
     │
 80s │ Generar reporte de faltantes
     │ ┌────────────────────────────┐
     │ │ • Create PDF report        │
     │ └────────────────────────────┘
     │
 90s │ Ensamblar PDF final
     │ ┌────────────────────────────┐
     │ │ • Crear portadas           │
     │ │ • Unir PDFs                │
     │ │ • Guardar final.pdf        │
     │ └────────────────────────────┘
     │
100s │ Subir a Google Drive
     │ ┌────────────────────────────┐
     │ │ • Upload final.pdf         │
     │ │ • Get public link          │
     │ └────────────────────────────┘
     │
110s │ Actualizar Google Sheet
     │ ┌────────────────────────────┐
     │ │ • Update cells E, F, G     │
     │ └────────────────────────────┘
     │
120s │ Response retornado
     │ ┌────────────────────────────┐
     │ │ PacketResponse             │
     │ │ status: "success"          │
     │ │ drive_folder_link: "..."   │
     │ │ final_pdf_link: "..."      │
     │ └────────────────────────────┘
     │
     ▼
   FIN
```

**Tiempo total:** ~2 minutos (varía según tamaño de archivos)

---

**Nota:** Este diagrama es una representación simplificada. Para más detalles, consulta el código fuente.
