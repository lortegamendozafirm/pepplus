preensamblado-service/
├── Dockerfile                  # Instrucciones para construir la imagen en Cloud Run
├── requirements.txt            # Librerías necesarias
├── .env                        # Variables de entorno (Credenciales Google, Configs)
└── app/
    ├── __init__.py
    ├── main.py                 # Punto de entrada de la aplicación (Inicializa FastAPI)
    ├── config.py               # Configuración global (Settings)
    ├── api/                    # Capa de API (Endpoints)
    │   ├── __init__.py
    │   └── v1/
    │       ├── __init__.py
    │       └── packet.py       # Endpoint principal del proceso
    ├── schemas/                # Modelos Pydantic (Estructura de los JSON de entrada/salida)
    │   ├── __init__.py
    │   └── request_models.py   # Qué esperamos recibir de App Script
    ├── services/               # Capa de Lógica de Negocio
    │   ├── __init__.py
    │   ├── orchestrator.py     # Coordina todo el proceso
    │   └── pdf_engine.py       # Lógica de conversión y unión de PDFs
    ├── integrations/           # Capa de Integración con Terceros
    │   ├── __init__.py
    │   ├── dropbox_client.py   # Cliente especializado de Dropbox
    │   └── google_client.py    # Cliente para Drive y Sheets (solo escritura)
    └── utils/                  # Utilidades generales
        ├── __init__.py
        └── helpers.py          # Normalización de texto, limpieza de archivos